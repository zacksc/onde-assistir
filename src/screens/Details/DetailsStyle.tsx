import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    fontSize: 30,
    fontWeight: '800',
    padding: 20,
    alignItems: 'center',
    textAlign: 'center'
  }
});